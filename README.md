# C-Notepad-Simulator
A console-based Notepad simulation in C++ using a doubly linked list and stack data structure. Supports text insertion, deletion, cursor movement, newline handling, and redo functionality while demonstrating custom implementation of dynamic data structures and text editor behavior.
